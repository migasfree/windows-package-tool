Thanks for downloading Windows Package Tool.

To install it, make sure you have these programs installed:

    * Python >= 3.9

Then run these commands from the command prompt:

    $ python -m build
    # pip install dist/<package>.whl

And enjoy!
